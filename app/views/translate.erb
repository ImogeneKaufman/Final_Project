<!DOCTYPE html>
<html>
  <head>
  <meta charset="UTF-8">
  <title>Polyglot</title>
  <link rel="stylesheet" type="css/style.css" href="text/css">
 </head>

   <h1> Welcome to Polyglot </h1>
  
  <% if @user %>
    <form method="post" action="/logout">
      <button type="submit">Log out</button>
    </form>
    <%end%>
    
    <form action="/translate" method ="POST">
  
     <select name="original_language">
    <%@languages[0].each do |language|%>
    <option value="<%=language[1]%>"><%=language[2]%></option>
    <%end%>
  </select>
  
  <select name="final_language">
    <%@languages[0].each do |language|%>
    <option value="<%=language[1]%>"><%=language[2]%></option>
    <%end%>
  </select>

     <input type="text" name="original_text" placeholder="Insert text to be translated">
     <input type="submit" value="Enter">
   </form>
  
  <%if @translation%>
  <%=@translation[0][0][0]%>
  <%end%>
  
  <ul>
    <%@all_translations = Translation.all%>
    <%@all_translations.each do |translation|%>
    <li> Original Text: <%=Translation.find(translation.original_text)%> -New Text: <%=Translation.find(translation.new_text)%> - Original Language: <%=Translation.find(translation.original_language)%> - New Language: <%=Translation.find(translation.new_language)%></li>
    <%end%>
  </ul>
<!--  where user_id is the same as the session user_id  -->

     </html>
